[theme]
primaryColor = "#2e7d32"    # A-SCEND style "Forest Green"
backgroundColor = "#FFFFFF" # Pure White background
secondaryBackgroundColor = "#F5F7FA" # Very light grey for cards (not sidebar anymore)
textColor = "#262730"       # Dark text
font = "sans serif"

[server]
enableCORS = false
